# Copy this file to .env (or set these as environment variables) and fill in your own values.
# Never commit the real .env file — it contains your bot's password.

# Your Discord bot token, from https://discord.com/developers/applications
# Bot tab -> Token -> Reset Token -> Copy
DISCORD_TOKEN=your_discord_bot_token_here
